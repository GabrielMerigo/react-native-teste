import React from 'react';
import { Text, View } from 'react-native';

export default function MinMax(){
  return(
      <View>
        <Text style={{ fontSize: 24 }}>O valor X é maior que o valor Y</Text>
      </View>
  )
}